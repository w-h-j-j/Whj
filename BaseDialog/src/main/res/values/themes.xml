<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--自定义dialog背景全透明无边框theme -->
    <style name="dialog_theme" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>    <!--背景颜色及和透明程度-->
        <item name="android:windowNoTitle">true</item>                             <!--是否去除标题 -->
        <item name="android:windowFrame">@null</item>                              <!--是否去除边框-->
        <item name="android:windowIsFloating">true</item>                          <!--是否浮现在activity之上-->
        <item name="android:backgroundDimEnabled">true</item>                      <!--是否模糊-->
    </style>

    <!--加载对话框时的动画-->
    <style name="dialog_anim">
        <item name="android:windowEnterAnimation">@anim/right_in_anim</item>       <!--进入时的动画-->
        <item name="android:windowExitAnimation">@anim/right_out_anim</item>       <!--退出时的动画-->
    </style>
</resources>